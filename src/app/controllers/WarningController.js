const { User } = require('../models/User');
const { Warning } = require('../models/Warning');

const WarningController = {
  store: (req, res) => {
    // const {email, }
  },
  list: (req, res) => {},
  map: (req, res) => {}
};
